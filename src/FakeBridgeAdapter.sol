/**
 * HXRO Bridge adapter for our very much real bridge provider
 */

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.18;

contract VeryRealBridgeProviderAdapter {
    function bridgeWithTheMostRealBridgeProvider(
        uint64 targetChainId,
        address srcToken,
        address destToken,
        uint256 amtIn,
        bytes memory payload
    ) external {
        // woo hoo look at me im bridgiiiiing
    }
}
